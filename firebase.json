{
  "firestore": [
    {
      "database": "(default)",
      "rules": "firestore.rules",
      "indexes": "firestore.indexes.json"
    },
    {
      "database": "global-notes-app-test",
      "rules": "firestore.rules",
      "indexes": "firestore.indexes.json"
    }
  ]
}
